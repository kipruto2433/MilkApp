# Public URL of the deployed MilkApp API (no trailing slash).
# Required for a hosted web build; localhost is only suitable for local development.
EXPO_PUBLIC_API_BASE_URL=https://your-api.example.com
